// SPDX-License-Identifier: MIT
pragma solidity ^0.8.26;

contract Mustafa {

    bool public ready = true;

    bool public completed = false;

    bool public comparision1 = 3 != 4;

    bool public comparision2 = 2 == 4;

    bool public comparision3 = (5 > 4) && (11 < 99);

    bool public comparision4 = (-4 <= -13) || (12 >= 99);

}

